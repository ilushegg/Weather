export interface WeatherMain {
  feels_like: number,
  grnd_level: number
  humidity: string,
  pressure: number,
  sea_level: number,
  temp: number,
  temp_kf: number,
  temp_max: number,
  temp_min: number,
}
